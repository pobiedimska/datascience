from dataset_structure import dataset

dataset += [{
          'room_type': 'Private room',
          'price' : 30,
          'weekly_price' : '$52.00',
          'beds' : 0
        }]

print(dataset)